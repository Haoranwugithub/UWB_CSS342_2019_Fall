#include "gtest/gtest.h"
#include "possible_parentheses.h"

TEST(test_recursive, 1) {
    possibleParenthesis(1);
}

TEST(test_recursive, 2) {
    possibleParenthesis(2);
}

TEST(test_recursive, 3) {
    possibleParenthesis(3);
}

